% =====================================================================
% TECT-Math438 -- G3'-b execution: (i) three-shell BCC ensemble race
%                 {110}+{200}+{211} and (ii) anisotropic per-axis {200}
%                 second harmonics -- both CLOSED-PASS; (iii) AddF N=64
%                 harmonic-ratio extraction registered with spec.
%
% Status:        EXECUTED -- 27/27 asserts, exit 0 (single sandbox call;
%                checkpoint armed, not needed). 135-pt three-shell scan
%                + 24-pt anisotropic-branch scan: 0 negatives; minima
%                reproduce the established single-shell/two-shell
%                records; refinement (cut20/64^3) margins hold.
%                NO tier action. Estimator-grade qualifier mandatory
%                (pinned scope language).
%
% Provenance: dispatcher-authored continuous-session turn (operator
%   standing directive; self-flagged S5 objection delta). All numbers
%   EXECUTED (Runs/math/Math438/g3prime_b_multishell_aniso.json).
% Date: 2026-06-04 UTC (bash-canonical).
% =====================================================================
% REVISION HISTORY (cumulative):
% v1.0 (2026-06-04): first issue (27/27 cold-run).
% v1.1 (2026-06-04, Math440 R3/F5/F6): the three mixed-shell moments
%   144/432/192 promoted from record to ASSERT in the script (6.3.8;
%   cold-run count now 30/30, exit 0); F6 caveat: the S5-alpha
%   mitigation chain's fourth leg cites the Math437 layer theorem,
%   whose proof was repaired in Math437 v1.1 (Math440 R1; F10 second
%   look pending) -- citation updated; the other three legs are
%   independent of it. No numerical content changed.
% =====================================================================

\section*{TECT-Math438. G3$'$-b: three-shell and anisotropic-harmonic ensemble races --- CLOSED-PASS}

\subsection*{1. Scope}

Math432 closed G3$'$ (two-shell $\{110\}{+}\{200\}$) and registered three
LOW-RISK residuals: (i) shells $\ge 3$ mixed-shell sextic dominance;
(ii) anisotropic per-axis second harmonics; (iii) the Math400-AddF
$N{=}64$ harmonic-ratio extraction. This note EXECUTES (i) and (ii) with
the Math432 engine generalised to arbitrary condensate fields (the
machinery is FFT/grid-based, hence condensate-shape-agnostic; engine
re-derived standalone with regression locks to Math430/432), and
REGISTERS (iii) with a concrete extraction spec. Anchored exact-Wick
Gibbs--Bogoliubov protocol throughout (Math429/430); estimator-grade
qualifier mandatory; no tier action.

\subsection*{2. Setup and new structural facts}

Condensate ansatz
\[
\phi_c = A_1\!\!\sum_{k\in\{110\}}\!\!e^{ikx}
+ \sum_{i=x,y,z} a_i\,(e^{i\,2 s\hat e_i x} + \mathrm{c.c.})
+ A_3\!\!\sum_{k\in\{211\}}\!\!e^{ikx},
\qquad s = q_0/\sqrt2,
\]
with $\{211\}$ the third BCC shell ($24$ vectors, $n_3 = 12$ pairs,
$|k|^2 = 3q_0^2$, $K_3 = r + 4Cq_0^4 = 0.861135$, off-sphere stiffness
$c_2^{(3)} = n_3[\,r_R + 4Cq_0^4\,] = 13.928$ --- the Math432 quoted
defence constant, here verified by finite difference to $1.2\%$, inside
the $5\%$ pre-registered window).

\paragraph{New exact resonance facts (grid-exact integers).} The
mixed-shell four-wave moments are ALL active:
\[
m_{31}^{\{110\}^3\{200\}} = 144, \qquad
\boxed{\,m_{31}^{\{110\}^3\{211\}} = 432\,}, \qquad
m_{211}^{\{110\}^2\{200\}\{211\}} = 192,
\]
i.e.\ the $\{211\}$ channel couples to the first shell THREE times more
strongly than the $\{200\}$ channel that drove the Math432 SHG tilt
(and $144$ here re-reproduces the Math432/Math437 cross-check
bit-exactly). The danger this gate probes is therefore real: the richest
resonance channel yet raced.

\subsection*{3. Results (all EXECUTED)}

\paragraph{(i) Three-shell race.} Grid $A_1 \in \{0.01, 0.0856, 0.14\}
\times A_2 \in \{-0.06, 0, 0.06\} \times A_3 \in \{-0.04, -0.01, 0,
0.01, 0.04\} \times M \in \{0.7, 1.0, 1.4\}M_R$ ($135$ points):
\[
\textbf{0 negatives}, \qquad
\min \Delta F = +1.9068\times10^{-4}\ \text{at}\ (0.01,\,0,\,0,\,M_R),
\]
the minimum sits at the PURE single-shell small-$A$ corner and equals
the Math430 record $+1.91\times10^{-4}$ --- every mixed-shell direction
is stiffer despite the $432$ resonance. Two-shell regression: the
$(0.01, +0.015, 0, M_R)$ point reproduces the Math432 scan minimum
$+5.536\times10^{-4}$ to five digits. Refinement at the minimum
(cut $12 \to 20$, grid $48^3 \to 64^3$): drift $-1.05\times10^{-7}$ =
margin$/1823$.

\paragraph{(ii) Anisotropic second harmonics.} Uniaxial $(\pm a, 0, 0)$,
biaxial $(\pm a, \pm a, 0)$ and isotropic $(\pm a)^{\times 3}$ branches,
$a \in \{0.03, 0.065\}$, $A_1 \in \{0.0856, 0.14\}$, $M = M_R$ ($24$
points): all positive; the branch minimum $+2.2546\times10^{-2}$ occurs
on the ISOTROPIC negative branch --- cubic-symmetry breaking RAISES the
energy at every probed amplitude (the Math432 G3$'$-b(ii) concern does
not materialise). Refinement drift at the branch minimum
$-1.05\times10^{-3}$ = margin$/21$.

\paragraph{(iii) AddF $N{=}64$ ratio extraction (registered, not
executed).} Spec: from the Math400-AddF $N = 64$ channel-state data,
extract the $\{200\}/\{110\}$ and $\{211\}/\{110\}$ Fourier-amplitude
ratios of the relaxed broken-phase configurations and compare against
the perturbative tilt prediction $A_2^* \approx -U m_{31} A_1^3 /
(2 c_2^{(2)})$ (and its $\{211\}$ analogue with $m_{31} = 432$,
$c_2^{(3)} = 13.93$); acceptance gate: measured ratios within a factor
$2$ of the tilt prediction at matched $A_1$. This is a data-extraction
task on archived run output, not a new race.

\subsection*{4. Consequences}

\begin{enumerate}
\item \textbf{G3$'$-b(i) and (ii): CLOSED-PASS} (estimator grade,
mandatory qualifier). G3$'$-b(iii) remains a registered data-extraction
item (LOW-RISK; not energetic).
\item The full gate ledger of the Reading-H attack chain is now:
G4, G1$'$, G1$''$-0/1$'$/2/3, G1$''$-3b (LAM+HEX executed), G3$'$,
G3$'$-b(i,ii), G6, Step-5 (isotropic-layer closure, Math437) --- every
energetic gate EXECUTED with zero surviving negatives anywhere in the
programme.
\item \textbf{Next mainline step is no longer a race}: consolidated
\S 15.5-class independent audit (Math433-pattern entry document covering
Math435--438) by a DIFFERENT agent/fresh session, then the operator's
tier/scope decisions. NO tier action here; Reading-H selection stays
T5 CLOSED@ESTIMATOR-GRADE pinned.
\end{enumerate}

\subsection*{5. Devil's-advocate self-test (CLAUDE.md 6.3.1 + 6.3.5(a))}

\textbf{Objection $\alpha$ (scan coarseness):} 135 + 24 points is a
coarse net; a negative pocket could hide between nodes.
\emph{VALID-with-mitigation}: the analytic small-amplitude bounds close
the small-$A$ region pattern-universally ($c_2 = n r_R$ per shell ---
lattice-independent, Math428 v1.1/Math431; $c_2^{(3)} = 13.93$ verified
here); the large-$A$ region is sextic-dominated with every probed mixed
moment positive-definite in the dressing; the Math437 layer theorem ---
as REPAIRED in its v1.1 (Math440 R1: four-interval proof with
anchor-only floors; F10 second look pending) --- independently bounds
the ENTIRE pattern class at the isotropic layer; and the scan minima
reproduce the
established records exactly, indicating no new structure. Residual risk
is confined to beyond-layer channels at intermediate $A$ --- the same
scope every executed race carries (pinned language).

\textbf{Objection $\beta$ (is the $432$ resonance handled exactly?):}
\emph{DISMISSED}: the engine computes ALL condensate moments by exact
band-limited FFT cell averages (Nyquist guards asserted per cut/grid);
$m_{31} = 432$ is itself a grid-exact integer output, and the W-coupling
matrix feeds the full $\phi_c^2$/$\phi_c^4$ Fourier content into the
Bloch determinant --- no truncation of the resonance channel occurs.

\textbf{Objection $\gamma$ (anisotropic probe set too small?):}
\emph{VALID-with-mitigation}: uniaxial/biaxial/isotropic $\times$ two
amplitudes $\times$ two $A_1$ covers the symmetry-distinct branch
topology at the dangerous amplitudes; the observed ordering (isotropy
lowest, symmetry breaking monotonically up) matches the Math427
diagonal-isotropy theorem's direction, and the margin at the branch
minimum is $21\times$ the refinement drift. A denser angular scan is
registered as an optional refinement, not a blocker.

\textbf{Objection $\delta$ (single-author continuous session):}
\emph{VALID-with-mitigation, self-flagged}: same provenance class as
Math437; 27/27 machine asserts + JSON; the consolidated \S 15.5 audit
(next step) covers this note.

\paragraph{Quantitative sanity checks (CLAUDE.md 6.3.4).}
\begin{itemize}
\item \textbf{Limit case}: $A_2 = A_3 = 0$ rows reproduce the
single-shell Math430 surface (min $+1.9068\times10^{-4}$ vs record
$+1.91\times10^{-4}$); $(0.01, 0.015, 0)$ reproduces Math432 to five
digits. PASS.
\item \textbf{Small-$A_3$ quadratic}: finite-difference $c_2^{(3)} =
14.10$ vs analytic $n_3(r_R + 4Cq_0^4) = 13.93$ ($1.2\%$, quartic
contamination at $A_3 = 0.005$). PASS.
\item \textbf{Integer identities}: $\langle\Phi_{110}^2\rangle = 12 =
2n_1$, $\langle\Phi_{211}^2\rangle = 24 = 2n_3$, per-axis
$\langle\Phi_{200,i}^2\rangle = 2$; $m_{31}$ moments land on integers to
$10^{-13}$. PASS.
\item \textbf{Sign-direction}: anisotropy raises energy (consistent with
Math427); mixed-shell admixture raises energy (off-sphere stiffness
dominates the $432$ resonance). PASS.
\item \textbf{Reproducibility}: 27/27 asserts, deterministic,
checkpointed, JSON artefact. PASS.
\end{itemize}

\subsection*{6. Pre-registered falsification-gate outcomes}

FAIL branch (anchored $\Delta F < 0$ confirmed at both resolutions):
\textbf{did not fire} --- no negative at any of the $159$ scanned points
at production resolution, and both extrema re-verified positive at
cut$20/64^3$. The Math424 Outcome-3 branch remains dormant.

\subsection*{7. Cross-references}

\texttt{Codes/supplementary/Math438\_g3prime\_b\_multishell\_aniso.py}
(v1.1: 30 asserts incl.\ the 144/432/192 moment asserts; FIRST
EXECUTION 2026-06-04 UTC, exit 0; checkpoint armed);
\texttt{Runs/math/Math438/g3prime\_b\_multishell\_aniso.json};
Math432 (+ engine; $m_{31} = 144$; G3$'$-b registration); Math430
(single-shell record); Math429 (anchored protocol); Math427 (isotropy
direction); Math426/435 (corrected anchors); Math436 (exact-Wick LAM/HEX
context); Math437 v1.1 (repaired layer theorem bounding the class);
Math400-AddF
(the $N{=}64$ data for the (iii) extraction).

% =====================================================================
% End of TECT-Math438 (260604-v1.1).
% =====================================================================
