/* DEPRECATED 2026-05-07: states_pillar_tiers.js has been renamed to
 * status_pillar_tiers.js. No HTML page loads this file any more; toe.html
 * now loads data/status_pillar_tiers.js. This stub is kept only so cached
 * references do not 404 — it does not define window.TECT_STATES_TIERS and
 * will be removed after one snapshot cycle. */
(function () {
  if (typeof console !== 'undefined' && console.warn) {
    console.warn('TECT: data/states_pillar_tiers.js is deprecated; the canonical file is data/status_pillar_tiers.js.');
  }
})();
